# Slash Bros 3D -- the proofs. This file imports the game (as Slash)
# and the claims (as Laws) and fills every law. The closed claims
# compute: the checker evaluates both sides and closes them by
# conversion. The route law holds by conversion too: the match on the
# back flag decides before the other flags are looked at.

import Base
import ./main.bend as Slash
import ./LAWS.bend as Laws

def Laws.dec_floor():
  {==}

def Laws.arrows_are_wasd():
  {==}

def Laws.pause_toggles():
  {==}

def Laws.back_to_title(fresh, pause, live, frozen, g):
  {==}
